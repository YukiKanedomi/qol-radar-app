QOLレーダーの定期収集（picks.json 追記モード・自動直接公開）。作業ディレクトリ c:/Users/kanedomi/Desktop/Claude/qol-radar-app。手順: (1) 5ジャンル(gadget/housework/work/health/service)で「買ってよかった/QOL向上/時短/効率化 2026」をWebSearch、YouTube限定検索(allowed_domains youtube.com)も併用し新顔を発掘。(2) 各候補を一般WebSearchでクロス確認し、複数ソースで裏が取れたものだけ採用。(3) data/picks.json の既存idと重複しない・出典2件以上・推しポイント(points)2〜3点を備えた良品だけを candidates.json(スクラッチパッド, UTF-8)にまとめ、英数kebabのidを付与。採用は1サイクル最大5件まで（1ジャンル1件目安）。特に推薦の強いものだけを厳選し、迷ったら落とす（翌日以降にまた拾える）。(4) `node scripts/add-picks.mjs <candidates.json>` で追記。added>0なら `git add data/picks.json` → コミット(末尾 Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>) → `git push origin main` で自動公開し、デプロイ確認は `gh run watch <id> --repo YukiKanedomi/qol-radar-app --exit-status > /dev/null 2>&1`（出力は必ず捨てて終了コードのみ）で成功を確認、追加id一覧を簡潔に報告。(5) 品質基準を満たす新顔が無ければ何もせず今サイクル終了（pushしない）。重複・誇張・出典の薄いものは採用しない。
